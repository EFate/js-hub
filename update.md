# 更新日志

## v1.5.4（2026-09-11）

### 🐛 修复

- **更新日志正文一行多个下载链接只出一个按钮**：此前按钮按「容器」去重，正文的同一个列表项里无论有几个下载链接都只给第一个配按钮，其余被跳过。现改为逐链接注入，每个下载链接旁都有独立的「镜像下载」按钮。

### ✨ 调整

- **更新日志正文注入独立开关**：正文里的下载链接此前与下方 Assets 附件列表共用「Release 附件」开关，无法单独关闭。现拆分为独立场景「更新日志正文」（面板内可单独开关，默认开启）；关闭后正文不再注入按钮，Assets 列表不受影响。
- 正文内按钮改为紧跟各自链接显示（此前挂在整行末尾，多链接行无法区分对应关系）。

## v1.5.3（2026-09-11）

### 🔧 注入与性能优化（无新功能）

- **适配 GitHub 全部三种页面导航方式**：此前只监听 Turbo 与旧版 pjax 导航事件，现补齐 React 软导航（soft-nav）事件——新版 GitHub 页面间跳转后，「镜像下载」按钮与 Release 增强显示的注入更快、更可靠；
- **更少后台观察器**：移除每个 Release 折叠块内部的重复 DOM 观察器（与全局监听双重覆盖），资产较多的 Release 页面开销更低，行为不变；
- **修复注入漏挂**：zipball / tarball 形式的源码包链接单独出现时，此前不会触发按钮注入扫描，现已覆盖；
- **架构图修复**：L7 芯片文字溢出修复，标注更新为「事件驱动」。

## v1.5.2（2026-09-11）


### ✨ 新增

- **自建节点**：面板「节点」页签可输入地址添加自己的加速节点，与内置/接口节点统一测速、勾选、参与下载候选；可随时删除，历史测速结果跨刷新保留。
- **地区徽章**：内置镜像池 32 源标注部署地区（美国 / 英国 / 香港 / 日本 / 多国），选节点时一目了然。
- **候选均衡策略**：下载时优先快档节点，但延迟相近的节点之间随机分流，避免所有用户集中打爆同几个最快公益源（公益源更稳定、不易被限速）。
- **全败降级窗口**：一轮镜像预检全部失败后的 60 秒内，后续下载跳过逐个预检、直接放行最快节点，省掉每次下载重复等待约 10 秒。

### 🐛 修复

- **不支持 HEAD 请求的镜像被永久误杀**：预检返回 405/501 不再计为节点故障。
- **错误节点混入候选**：测速时把网关错误（5xx）/ 限流（429）的响应当成"低延迟好节点"的问题，已改为正确判定。
- **"全不选"被覆盖**：手动全不选节点后刷新页面，勾选不再被静默改回"最快 10 个"；节点列表为空时也不会清空已保存的勾选。
- **升级丢勾选**：旧版本勾选的节点（URL 带尾斜杠）升级后不再丢失，自动归一化匹配。
- **重置不彻底**：「重置全部设置」现在会一并清除自建节点。
- **长会话卡顿隐患**：release 区块的事件监听器在页面跳转后正确释放，不再累积僵尸 DOM；测速的安全网定时器用完即回收。
- **统计显示错误**：节点页摘要不再把"未测速"当成 99999ms 算进最快/平均延迟；全部未测速时显示"待测速"。
- **启动器丢失**：跨屏拖拽保存的位置在窗口缩小/换显示器后会自动拉回可视区域，按钮不再消失。
- **附属文件重排错乱**：release 全部为校验/附属文件时，二次排序不再把内容写错容器。

### 🧹 优化

- **更少网络请求**：删除了每个节点缓存周期都无条件拉取节点列表的常驻定时器，仅在缓存真正过期时才请求。
- **去除冗余**：清理无引用的样式规则、无效字段与重复代码，下载按钮逻辑复用统一实现。

